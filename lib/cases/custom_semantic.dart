// TODO remettre le custom semantic ici du radio button liste ?
import 'package:flutter/material.dart';
import 'package:flutter_accessibility_demo/utils/layout.dart';

class CustomSemanticRadioPage extends StatelessWidget {
  const CustomSemanticRadioPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return DemoLayout(
        title: 'Custom Semantic Radio',
        body: Column(
          children: [],
        ));
  }
}
